<h1>BURGERS!</h1>

<img src="/assets/img/burgerlogger_photo.png" alt="burger">

<h2>BURGERS TO EAT</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>DEVOURED BURGERS</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>ADD A BURGER</h2>
<form class="create-form">

  <div class="form-group">
    <label for="new-burger">YOUR BURGER:</label>
    <input type="text" id="new-burger" name="name">
  </div>

  <button type="submit">SUBMIT BURGER</button>
</form>

